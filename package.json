{
  "name": "wdd-x99",
  "version": "1.0.0",
  "description": "Date ideas web app",
  "scripts": {
    "build:css": "postcss public/styles/styles.source.css -o public/styles/styles.css",
    "watch:css": "postcss public/styles/styles.source.css -o public/styles/styles.css --watch",
    "deploy": "npm run build:css && firebase deploy --only hosting"
  },
  "devDependencies": {
    "lightningcss-cli": "^1.31.1",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1",
    "postcss-nested": "^7.0.2"
  }
}
